"""Stage 3 menu deep-dive — out of scope for Phase 1. Stub per SPEC.md.

Will fetch homepage + menu/private-event pages, route by content type
(HTML/PDF/image/JS-shell/Photos fallback), and make ONE Claude extraction
call per venue against the strict JSON schema in CLAUDE.md. Deterministic
price rules stay in code.
"""
