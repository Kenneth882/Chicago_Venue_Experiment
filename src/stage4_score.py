"""Stage 4 master scoring — out of scope for Phase 1. Stub per SPEC.md.

Will apply hard gates, compute the weighted score in code (zone tier, price
fit, rating*log(reviews), capacity fit, contact method, multi-query bonus),
rank, take top 500, write stage=0_sourced, and export CSV + audit sample.
"""
